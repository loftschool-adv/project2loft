extends ../layout

block content
		div.wrapper
			div.flipper-container
				div.flipper
					div.front
						div.welcome
							div.welcome__title Добро пожаловать
							div.welcome__text Перед вами сервис, который поможет вам организовать свои фотографии в альбомы и поделиться ими со всем миром!
						div.popup
							form.popup__form.popup__form-login(action="")
								.popup__input-container
									label.popup__label(for="mail")
										svg.popup__svg
											use(xlink:href='/img/sprite.svg#envelope') 
									input.popup__input(type="email" name="mail" placeholder="Электронная почта")
								.popup__input-container
									label.popup__label(for="password")
										svg.popup__svg
											use(xlink:href='/img/sprite.svg#password') 
									input.popup__input(type="password" name="password" placeholder="Пароль")
								a(href="#").popup__link.popup__link_forget-pass Забыли пароль?
								div.popup__error.hide  E-mail или пароль не верен
								input.popup__submit(type="submit" value = "Войти")
								div.popup__registration Нет аккаунта? 
									a(href="#").popup__link.popup__link_registr Зарегистрироваться
					div.back
						div.welcome
								div.welcome__title Регистрация
						div.popup
							form.popup__form.popup__form-registration(action="" method="POST")
								.popup__input-container
									label.popup__label(for="mail")
										svg.popup__svg
											use(xlink:href='/img/sprite.svg#name') 
									input.popup__input(type="email" name="user" placeholder="Имя")
								.popup__input-container
									label.popup__label(for="mail")
										svg.popup__svg
											use(xlink:href='/img/sprite.svg#envelope') 
									input.popup__input(type="email" name="mail" placeholder="Электронная почта")
								.popup__input-container
									label.popup__label(for="password")
										svg.popup__svg
											use(xlink:href='/img/sprite.svg#password') 
									input.popup__input(type="password" name="password" placeholder="Пароль")
								div.popup__promt  Ваши данные остаются строго конфедициальны
								input.popup__submit(type="submit" value = "Создать аккаунт")
								div.popup__registration Уже зарегистрированы? 
									a(href="#").popup__link.popup__link_enter Войти
			footer.main-footer 2016 создано командой профессианалов на продвинутом курсе по веб-разработке от Loftschool
